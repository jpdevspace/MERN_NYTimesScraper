import React, { Component } from 'react';

class Saved extends Component {
  render() {
    return (
        <section>
            <h1>Saved</h1>
        </section>
    );
  }
}

export default Saved;